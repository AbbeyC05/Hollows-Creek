(()=>{
function exactCard(name){const biz=document.getElementById('businesses');if(!biz)return null;return [...biz.querySelectorAll('.biz-card,.business-card,button')].find(x=>(x.querySelector('strong')?.textContent||'').trim().toLowerCase()===name.toLowerCase())||null}
function addCard(name,small,desc,target,key){const biz=document.getElementById('businesses');if(!biz)return;let card=exactCard(name);if(!card){const grid=biz.querySelector('.biz-grid');if(!grid)return;card=document.createElement('button');card.className='biz-card';grid.appendChild(card)}card.dataset[key]='1';card.onclick=()=>showPage(target);card.innerHTML=`<small>${small}</small><strong>${name}</strong><p>${desc}</p>`}
function normalize(){const biz=document.getElementById('businesses');if(!biz)return;const cards=[...biz.querySelectorAll('.biz-card,.business-card,button')];const elan=cards.filter(x=>/^É?LAN$/i.test((x.querySelector('strong')?.textContent||'').trim()));if(elan.length>1)elan.slice(1).forEach(x=>x.remove());addCard('ÉLAN','Dance Studio • Libby & Riley Beaumont','Dance training, performance and creative development in Hollow\'s Creek.','elanStudio','elanStudioFinal');addCard('RILEY','Balletwear • Riley Beaumont','Balletwear, rehearsal layers, performance pieces and dancer essentials.','rileyByRiley','rileyBrandFinal')}
setTimeout(normalize,700);setTimeout(normalize,1800);setTimeout(normalize,3500);setTimeout(normalize,4700);
})();